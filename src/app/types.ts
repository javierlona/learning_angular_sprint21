export interface Todo {
  id: string,
  userId: number,
  title: string,
  completed: boolean
}

/*
* You can add more data types into this file
*
*/
export interface Album {
  userId: number,
  id: number,
  title: string,
  band: string
}
