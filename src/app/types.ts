export interface Todo {
  id: number,
  userId: number,
  title: string,
  completed: boolean
}

/*
* You can add more data types into this file
*
*/
export interface Album {
  userId: number,
  idd: number,
  title: string,
  band: string
}
